apiVersion: v1
kind: Service
metadata:
  name: {{ include "semb-stack.fullname" . }}
  labels:
    {{- include "semb-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "semb-stack.selectorLabels" . | nindent 4 }}
