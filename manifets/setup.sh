#!/usr/bin/env bash

set -exEuo pipefail

# Trap -e errors
trap 'echo "Exit status $? at line $LINENO from: $BASH_COMMAND"' ERR

kind create cluster --kubeconfig .kubeconfig.yaml --config kind/config.yaml
